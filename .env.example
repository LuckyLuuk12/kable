# Azure Application Configuration
# Create an Azure App Registration and get these values from the Azure Portal
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_REDIRECT_URI=http://localhost:43110/callback

# Optional: Add client secret if using confidential client (not recommended for desktop apps)
# AZURE_CLIENT_SECRET=your_client_secret_here

# Optional: Custom port for OAuth callback server
OAUTH_PORT=43110
